apply plugin: 'java'
apply plugin: 'jacoco'

description = 'Ethanol water dilution'

repositories {
    jcenter()
}

test {
  testLogging.showStandardStreams = true
}

dependencies {
    testCompile 'junit:junit:4.11'
}
